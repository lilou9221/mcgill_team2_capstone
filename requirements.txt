# Python dependencies for Biochar Suitability Mapper
# For installation instructions, see README.md

# Core data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=10.0.0  # For Parquet file support (DataFrame cache)

# Geospatial processing
# NOTE: On Windows, install via Conda first:
# conda install -c conda-forge geopandas rasterio shapely fiona pyproj gdal
geopandas>=0.14.0
shapely>=2.0.0
rasterio>=1.3.0
fiona>=1.9.0
pyproj>=3.6.0

# Spatial indexing
h3>=3.7.0

# Visualization
matplotlib>=3.7.0
pydeck>=0.8.0

# Configuration
pyyaml>=6.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0  # For downloading data from Cloudflare R2
gdown>=5.0.0  # For downloading data from Google Drive (fallback)

# Streamlit UI
streamlit>=1.38.0
